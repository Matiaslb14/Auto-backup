# Directorios a respaldar (puedes sumar o quitar)
BACKUP_SOURCES=("$HOME/linux-projects")

# Dónde guardar los .tar.gz (dentro del proyecto para evitar permisos)
BACKUP_DEST="$PWD/backups"

# Prefijo para los nombres de archivo
BACKUP_PREFIX="backup"

# Días que se mantienen los backups antes de borrarlos
RETENTION_DAYS=7

# (Opcional) Correo para notificación. Requiere 'mail' funcionando (mailutils/postfix/msmtp)
EMAIL_TO="lagos.barra.m@gmail.com"

# (Opcional) archivo con patrones a excluir
EXCLUDES_FILE="$PWD/excludes/patterns.txt"
